<li class="job-member-list-item" id="job_membership_<%= job_membership.id %>">
  <%= link_to job_membership.user.name, job_membership.user, class: "activity-user-link " %>
  <p class="job-members-position-title"><%= job_membership.user.position_title %></p>

  <%= link_to "remove", job_membership, method: :delete, remote: true, data: {confirm: "Remove " + job_membership.user.name + " from job?"},
              class: "delete-button-small job-member-button" %>

</li>