<%= link_to "job", job %>
<% if !inside_job %>
    <p class="inline"> (<%= job.job_template.product_line.name %>
      | <%= job.job_template.name %>
      on <%= link_to job.field.name, job.field %>
      | <%= link_to job.well.name, job.well %>)
    </p>
<% end %>