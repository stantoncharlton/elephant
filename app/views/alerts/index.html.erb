<% provide(:title, I18n.t("alerts.index.title")) %>

<div class="page_header">
  <h1><%= t ".title" %></h1>

  <h2><%= @new_alerts.count() %></h2>
</div>

<div id="new_alerts" class="list">
  <%= render @new_alerts %>
</div>

<% if @new_alerts.empty? %>
    <div class="no-alerts">
      <p class="center"><%= t ".no-new" %></p>
    </div>
<% end %>


<% if !@old_alerts.empty? %>

    <div class="page_header old-alerts-div">
      <h1><%= t ".past" %></h1>

      <h2><%= @old_alerts.count() %></h2>
    </div>


    <div id="old_alerts" class="list">
      <%= render @old_alerts %>
    </div>
<% end %>


<% if @alerts.nil? || @alerts.empty? %>
    <div class="no-alerts">
      <p class="center"><%= t ".none" %></p>
    </div>
<% end %>