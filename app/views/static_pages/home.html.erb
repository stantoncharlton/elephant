<% if signed_in? %>
    <% provide(:title, 'Home | ' + current_user.name) %>
    <p>Logged in</p>

<% else %>

    <% provide(:title, 'Home') %>

    <div>

      <%= render 'sessions/signin' %>

      <div class="quote">
        <p>Reduce non-productive time and increase efficiency, all with a <a class="link" href="/about">comprehensive solution</a></p>
        <br>
        <a href="tel:1-817-382-7776" class="phone">1-817-382-7776</a>
      </div>

    </div>


<% end %>

