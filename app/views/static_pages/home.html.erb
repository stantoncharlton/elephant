<% if signed_in? %>
    <% provide(:title, 'Home | ' + current_user.name) %>
    <p>Logged in</p>

<% else %>

    <% provide(:title, 'Home') %>

    <div>

      <%= render 'sessions/signin' %>

      <div class="quote">
        <p>Get ready to streamline oilfield operations with the new face of power, security, and efficiency.</p>
        <%= link_to "Learn more about Elephant", about_path %>
      </div>

    </div>


<% end %>

