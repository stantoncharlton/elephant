<% if signed_in? %>
    <% provide(:title, 'Home | ' + current_user.name) %>
    <p>Logged in</p>

<% else %>

    <% provide(:title, 'Home') %>

    <div>

      <%= render 'sessions/signin' %>

      <div class="quote">
        <p>“Smart technology to streamline, protect, and empower the organization.”</p>
        <%= link_to "Learn more about Elephant", about_path %>
      </div>

    </div>


<% end %>

