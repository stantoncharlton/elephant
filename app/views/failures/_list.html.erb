<%= form_for :failures, url: failures_path, remote: true do |f| %>

    <div class="page_header failure-page-header ">
      <h1>Performance</h1>

      <p>Please rate the quality and performance of the job</p>
    </div>

    <%= hidden_field_tag "performance_rating", name: "rating" %>
    <div>
      <div class="rating-level"><p>poor &nbsp;</p></div>

      <div class="inline">
        <a href="#" class="star-button" level="1">1</a>
        <a href="#" class="star-button" level="2">2</a>
        <a href="#" class="star-button" level="3">3</a>
        <a href="#" class="star-button" level="4">4</a>
        <a href="#" class="star-button" level="5">5</a>
      </div>

      <div class="rating-level"><p>&nbsp; excellent</p></div>

    </div>

    <br>

    <div class="page_header failure-page-header ">
      <h1>Failures</h1>

      <p>Please mark any failures that occurred</p>
    </div>

    <%= f.hidden_field :job_id, value: job.id %>

    <% job.job_template.failures.each do |failure| %>
        <fieldset class="field">

          <div class="failure-checkbox-div pull-left">
            <%= f.check_box failure.id.to_s, checked: (job.failures.find_by_failure_master_template_id(failure.failure_master_template.id) ? true : false),
                            class: "regular-checkbox", id: "checkbox_" + failure.id.to_s %>
            <label for="checkbox_<%= failure.id.to_s %>"></label>
          </div>

          <p class="failure-checkbox-text"><b><%= failure.failure_master_template.text %></b></p>
        </fieldset>
    <% end %>

    <div class="content actions">
      <%= f.submit "Save", class: "bluebtn pull-right" %>
    </div>
<% end %>