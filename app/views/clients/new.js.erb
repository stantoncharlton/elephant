
$('#modal_popup').find('.modal-content').append("<%= j render 'clients/new_client' %>");
$('#modal_popup').css('visibility', 'visible');
$('#modal_popup').find('.loading').addClass('hidden');

$('#client_name_id').keyup(function() {
    $.ajax({
        url:"/clients?search=" + $(this).val(),
        type:"GET",
        dataType:"script"
    });
});
