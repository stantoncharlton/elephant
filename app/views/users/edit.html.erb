<% provide(:title, "Update user") %>
<h1>Update user</h1>

<%= form_for(@user) do |f| %>
    <%= render 'fields', f: f %>
    <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
<% end %>
